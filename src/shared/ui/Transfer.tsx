// import {NumericField} from '@shared/ui/NumericField.tsx';
// import {useState} from 'react';

type Props = {};

export function Transfer(props: Props) {
	const {} = props;

	// const [firstItemValue, setFirstItemValue] = useState<string>('');
	// const [secondItemValue, setSecondItemValue] = useState();

	return (
		<div>
			Transfer
			{/*<NumericField value={firstItemValue} onChange={setFirstItemValue} />*/}
			<div>to</div>
		</div>
	);
}
